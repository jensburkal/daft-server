Daft.Server 3.5.1 Release Notes

This is primarily a bug fix release

Changes from Release 3.5:

- You can now get a string list value back in JSON and .NET API
- .NET API has CumulusStringEnum class to support string list support
- File Monitor samples have watch folder pointing to demo.daft-solutions.com, to support testinf from Daft Server


Bug Fixes:

- Download of assets stored in a Cumulus Vault now works
- File Monitor functions if no preview, or metadata sections defined in config file
- File Monitor works correctly if asset or metadata files are dropped in watch folder, while processing of other files is in progress

Notes:

